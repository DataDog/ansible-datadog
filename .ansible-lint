---
enable_list:
  - empty-string-compare
  - fqcn-builtins
  - fqcn[action]

warn_list:
  - var_naming
  - idiom
  
var_naming_pattern: "^[a-z_][a-z0-9_]*$"

skip_list:
  - var-naming[no-role-prefix]
